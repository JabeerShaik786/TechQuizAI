function EditProfileModal({ open, onClose, formValues, setFormValues, avatarPreview, avatarFile, uploadError, dragActive, inputRef, handleFileChange, handleDragEvents, handleDrop, savingProfile, onSave, onRemove, onCancel }) {
  if (!open) return null;
  return /* @__PURE__ */ React.createElement("div", null, "test");
}
