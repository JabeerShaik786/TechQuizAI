# Frontend environment variables for Vite
# These are safe to commit because they contain only endpoints or public configuration.

VITE_API_URL=http://localhost:5000/api
VITE_AUTH_REDIRECT_URI=http://localhost:3001/login
VITE_APP_NAME=TechQuizAI
